<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="images"
        android:summary="@string/pref_description_image"
        android:title="@string/pref_title_image" />
    
    <CheckBoxPreference
        android:defaultValue="false"
        android:key="screen"
        android:summary="@string/pref_description_screen"
        android:title="@string/pref_title_screen" />

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="english"
        android:entries="@array/pref_language_list_titles"
        android:entryValues="@array/pref_language_list_values"
        android:key="language_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_language" />

</PreferenceScreen>
